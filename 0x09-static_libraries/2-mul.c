#include "main.h"
/**
*mul -Multiplies 2 intergers
*@a: that's the first one
*@b: that's the other
*Return: the product of a and b
*/

int mul(int a, int b)
{
int result;

result = (a * b);
return (result);
}
