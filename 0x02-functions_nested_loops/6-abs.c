#include "main.h"

/**
*_abs -  prints the sign of a number.
*@n: The interger  size to be checked
*Return: eiter 0 or 1
*/

int _abs(int n);
{int value;
if (n < 0)
{
return (n * -1);
}
else
{return (n);
}
}

