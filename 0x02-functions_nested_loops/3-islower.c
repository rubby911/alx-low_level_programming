#include "main.h"

/**
* _isalpha - Checlks if a given letter is lower
*c - letter to check
*Return: eiter 0 or 1
*/

int _isalpha(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
